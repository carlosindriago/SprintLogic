package com.sprintlogic.auth;

import com.sprintlogic.db.*;
import java.util.*;

public class User {
    public void login() {
        Database db = new Database();
        db.connect();
        List<String> list = new ArrayList<>(); 
    }
}
